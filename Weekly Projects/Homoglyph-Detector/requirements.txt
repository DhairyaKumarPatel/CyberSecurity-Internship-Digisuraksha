# Homoglyph Detector Tool - Requirements
# This tool uses only Python standard library modules
# No external dependencies required

# Minimum Python version: 3.6+
# The following standard library modules are used:
# - argparse (command line argument parsing)
# - sys (system-specific parameters and functions)
# - json (JSON encoder and decoder)
# - re (regular expression operations)
# - typing (type hints support)
# - urllib.parse (URL parsing utilities)
# - unicodedata (Unicode character database)

# Optional: For development and testing
# - pytest>=6.0  # For advanced testing (optional)
# - black>=21.0  # For code formatting (optional)
# - flake8>=3.8  # For code linting (optional)
